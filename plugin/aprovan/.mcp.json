{
  "mcpServers": {
    "aprovan": {
      "command": "npx",
      "args": ["-y", "@aprovan/mcp-plugin", "serve"],
      "env": {
        "APROVAN_MCP_URL": "${APROVAN_MCP_URL}",
        "APROVAN_MCP_TOKEN": "${APROVAN_MCP_TOKEN}"
      }
    }
  }
}
