Backend-only / tooling change; no user-facing surface. The only user-visible effect is the loss of
the visual-edit (bobbin) overlay button inside the Edit modal — a confirmed deletion, not a UX
flow to design (the AI text-edit loop in the same modal is unaffected and needs no new UX).
