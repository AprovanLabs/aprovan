# Brief: Slug rules and slug registries

## Mission

Create `server/workspace/src/apps/slugs.ts` — the single choke point for slug
shape validation (IW-9 decision D4) plus the global slug claim registry that
lets published apps reserve a deployment-wide vanity slug, and a
workspace-slug resolver stub. When you are done, `assertValidSlug` rejects
anything ULID-shaped using the codebase's one existing definition of "ULID"
(never a second regex), `claimGlobalSlug`/`releaseGlobalSlug`/
`resolveGlobalSlug` give stream 5's `/a/<globalSlug>` vanity route something
real to resolve against, and `resolveWorkspaceSlug` gives stream 5's
`/w/<wsSlug>/...` route a (currently-always-empty, and that's fine) resolver
to call. Streams 3 and 5 both depend on this module's exports; ship it first.

## Read first

All paths relative to `/Users/jacob/Documents/Code/AprovanLabs/aprovan`:

1. `openspec/changes/IW-9-APP-FIRST.md` — decision D4
2. `docs/decisions/0002-app-first-platform-invariants.md`
3. `openspec/changes/iw9-f4-app-identity/prd.md` — the workspace-slug
   assumption in "Constraints & Assumptions" (no `wsSlug` concept exists
   today, verified by grep)
4. `openspec/changes/iw9-f4-app-identity/tech-plan.md` — Context (`NAME_RE`
   location), T4, T6, and the "Slug + claims" block under "Interfaces &
   Data" (frozen — implement exactly this shape)
5. `openspec/changes/iw9-f4-app-identity/specs/app-slug/spec.md` — full spec
   (reproduced under Acceptance criteria below; note the two requirements
   about directory-basename-as-slug and workspace-unique rename belong to
   stream 3, not this stream — this stream owns shape rules and the global
   claim registry)
6. `server/workspace/src/apps/store.ts:167` — `NAME_RE` (reuse verbatim, do
   not redefine)
7. `server/workspace/src/apps/identity.ts` — full file (122 lines); reuse
   `isAppId` (line 36-38) and `DEPLOYMENT_TENANT` (line 90); copy the
   `AppLocationRecord`/`indexAppLocation`/`dropAppLocation`/
   `resolveAppLocation` (lines 93-121) pattern as the template for the new
   `svc#slugs/*` and `svc#wsSlugs/*` scopes — do not edit this file (that is
   stream 3's job, adding a *different* new scope, `svc#apps/root`)
8. `server/workspace/src/apps/directory.ts:35-43` — `assertNotDeploymentTenant`
   pattern, useful reference if you want the same defensive guard on the new
   deployment-tenant scopes (optional, not required by the tasks)

## Tasks

(Verbatim from `openspec/changes/iw9-f4-app-identity/tasks.md` §2 — unchanged
by the pre-dispatch repair pass)

> Depends-on: - | Repo: aprovan | Touches: aprovan/server/workspace/src/apps/slugs.ts, aprovan/server/workspace/tests/app-slugs.test.ts | Verify: pnpm --filter @aprovan/workspace test -- tests/app-slugs.test.ts && pnpm --filter @aprovan/workspace typecheck

- [ ] 2.1 Create `server/workspace/src/apps/slugs.ts` with `assertValidSlug`: existing `NAME_RE` shape (`apps/store.ts:167`) AND NOT `isAppId(slug)` from `apps/identity.ts:36-38` — one ULID definition, no second regex (tech-plan T4; spec app-slug "ULID-shaped slug rejected", "26-char non-base32 slug accepted").
- [ ] 2.2 Implement the global slug claim registry on `svc#slugs/<globalSlug>` under `DEPLOYMENT_TENANT` (pattern of `svc#apps/byId`, `identity.ts:90-91`): `claimGlobalSlug` (409 naming the holder when taken), `releaseGlobalSlug` (holder-only), `resolveGlobalSlug` (tech-plan T6; spec app-slug "claim granted once").
- [ ] 2.3 Implement `resolveWorkspaceSlug(wsSlug)` reading `svc#wsSlugs/<wsSlug>` under `DEPLOYMENT_TENANT`; resolver only — nothing in F4 writes entries, unresolved returns undefined (tech-plan T6; PRD assumption: no ws-slug exists today).
- [ ] 2.4 New test file `tests/app-slugs.test.ts`: shape rules incl. ULID-shape fixtures (a real `ulid()` output rejected; 26-char strings with `u`/`i`/`l`/`o`/hyphen accepted), claim/409/release lifecycle, holder-only release, unresolved wsSlug.

## Acceptance criteria

Verbatim from `specs/app-slug/spec.md` (the two requirements this stream owns):

### Requirement: Slug shape rules
Slugs SHALL match the existing app-name shape (lowercase, alphanumeric plus hyphen, starting alphanumeric, at most 64 chars — `NAME_RE` in `apps/store.ts`), with one added exclusion: any slug consisting of exactly 26 Crockford base32 characters (the ULID shape, case-insensitive) SHALL be rejected, so slugs and ULIDs remain disjoint namespaces and reference resolution (`resolveAppRef`) stays unambiguous.

#### Scenario: ULID-shaped slug rejected
- **WHEN** a slug of exactly 26 Crockford base32 characters (e.g. `01arz3ndektsv4rrffq69g5fav`) is submitted
- **THEN** validation fails with an error stating that ULID-shaped slugs are reserved

#### Scenario: ordinary slug accepted
- **WHEN** a slug like `team-recipes` is submitted
- **THEN** validation passes

#### Scenario: 26-char non-base32 slug accepted
- **WHEN** a 26-character slug containing a character outside the Crockford base32 alphabet (e.g. containing `u`, `i`, `l`, `o`, or a hyphen) is submitted
- **THEN** validation passes (only the exact ULID shape is excluded)

### Requirement: Global slug claim registry for published apps
The platform SHALL provide an optional global slug claim registry (deployment-scoped, alongside the existing `svc#directory` index) mapping a globally unique slug to one `appId`. Only published (directory-visible) apps MAY hold a claim; claims obey the same shape rules including ULID-shape rejection; a claim SHALL be releasable by its holder and SHALL be dropped when the app is unpublished or removed. Vanity URL resolution (see app-url-scheme) consults this registry.

#### Scenario: claim granted once
- **WHEN** a published app claims a free global slug
- **THEN** the registry binds it to that `appId`, and a second app's claim of the same slug fails with 409

#### Scenario: unpublish releases claim
- **WHEN** an app holding a global slug claim is unpublished or removed
- **THEN** the claim is released and the slug becomes claimable

(The *trigger* for "unpublish releases claim" — wiring `releaseGlobalSlug`
into the actual unpublish/remove code path — is stream 3's task 3.7. This
stream's test file proves `releaseGlobalSlug` behaves correctly as a
standalone primitive; it does not need to exercise the unpublish flow
itself.)

The two remaining `app-slug` requirements ("Directory name is the vanity
slug", "Workspace-unique slugs with rename as alias move") are reconcile-time
concerns owned by stream 3 — do not implement workspace-scoped alias
uniqueness here; this stream builds only the shape rule and the *global*
(deployment-wide) claim registry.

## Verify

Run from `/Users/jacob/Documents/Code/AprovanLabs/aprovan`:

```bash
pnpm turbo run build --filter=@aprovan/workspace
pnpm --filter @aprovan/workspace test -- tests/app-slugs.test.ts
pnpm --filter @aprovan/workspace typecheck
```

The first line is a correction over `tasks.md`'s literal `Verify:` string
(see `briefs/deviations.md` §9) — turbo's `test`/`typecheck` tasks declare
`dependsOn: ["^build"]` for `@aprovan/native`/`@aprovan/node`/
`@aprovan/patchwork`, which a bare `pnpm --filter` call bypasses. Cached and
cheap when nothing changed. All commands must exit 0.

## Constraints

- Implement only what the tasks say; the function signatures in
  `tech-plan.md`'s "Slug + claims" interface block are fixed — if one seems
  wrong, stop and report instead of changing it.
- Surgical changes only; match existing style (see karpathy-guidelines skill).
- Do not edit `apps/identity.ts` or `apps/store.ts` — read-only references
  for this stream (stream 3 owns edits to both).
- Do not build workspace-scoped alias uniqueness or directory-basename
  binding here — that is reconcile's job (stream 3), not this module's.
- Do not modify files outside: `server/workspace/src/apps/slugs.ts`,
  `server/workspace/tests/app-slugs.test.ts`.

## Model

**Sonnet** — the default tier for every `iw9-f4` stream per
`IW-9-EXECUTION-OVERVIEW.md`'s "Model tiers for the implementing fleet"
table. F4 does not appear in that table's Opus-escalation row; this stream
reuses an existing pattern (`AppLocationRecord`/`indexAppLocation`) for a new
scope, fully specified by tech-plan T4/T6 — elaboration against a frozen
contract. Haiku is not used in this fleet (unavailable); do not downgrade
below Sonnet regardless. Do not escalate to Opus.

## Report back

When done: check off tasks 2.1–2.4 in
`openspec/changes/iw9-f4-app-identity/tasks.md`, and open a PR (or write
`briefs/02-report.md`) containing: what you built, how you verified it, any
deviations from this brief and why, and anything streams 3 and 5 (which
depend on `slugs.ts`'s exports) need to know.
