- Resolve dependencies in AprovanLabs/core vs AprovanLabs/registry & AprovanLabs/aprovan.
    - Note I have some eslint and prettier config in AprovanLabs/aprovan now that used to be a `@aprovan` package in core. Since deprecating core, not everything was brought over
    0 Simplify, consolidate, and make sure packages are exposed for other Aprovan repos
