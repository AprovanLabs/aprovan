/**
 * @deprecated Import from `@aprovan/editor/ts` instead.
 * Thin re-export so existing consumers keep resolving during the coordinated release.
 */
export {
  AMBIENT_FALLBACK,
  AMBIENT_FALLBACK_PATH,
  createTypeEnvironment,
  loadTsEnvironment,
  TsScriptEditor,
  createChecker,
  createProjectChecker,
  type TypeEnvironment,
  type TypeEnvironmentOptions,
  type TsScriptEditorProps,
  type CheckerFactoryOptions,
} from "@aprovan/editor/ts";